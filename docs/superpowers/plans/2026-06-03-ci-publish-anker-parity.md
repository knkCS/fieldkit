# CI/Publish Anker Parity Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bring `@knkcs/fieldkit`'s CI and publish GitHub Actions to parity with `@knkcs/anker` — add an export-surface guard, run full checks before publishing, publish to both npmjs.org (with provenance) and GitHub Packages, and switch the release tag scheme to `v*`.

**Architecture:** Port anker's repo-agnostic `verify-exports.ts` script (parses `tsup.config.ts` entries, diffs source exports against built `.d.ts`), wire it into `package.json` and CI, then rework the publish workflow to gate on full checks and dual-publish. No application/runtime code changes.

**Tech Stack:** GitHub Actions, npm, tsup, tsx, TypeScript compiler API (used by the ported script).

**Reference spec:** `docs/superpowers/specs/2026-06-03-ci-publish-anker-parity-design.md`

---

## File Structure

- `scripts/verify-exports.ts` — **new**, ported verbatim from anker. Self-contained Node/tsx script; the only new source file. One responsibility: assert every entry's source export surfaces in its built `.d.ts`.
- `package.json` — **modify**. Add `tsx` devDep + `verify-exports` script.
- `.github/workflows/ci.yml` — **modify**. Add one `verify-exports` step.
- `.github/workflows/publish-fieldkit.yml` — **modify**. New trigger, permissions, full check gate, dual publish.
- `package-lock.json` — **regenerated** by `npm install` when `tsx` is added.

---

## Task 1: Port the `verify-exports` script and wire it into package.json

**Files:**
- Create: `scripts/verify-exports.ts` (copied from `../anker/scripts/verify-exports.ts`)
- Modify: `package.json` (add `tsx` devDep + `verify-exports` script)
- Modify: `package-lock.json` (regenerated by npm)

- [ ] **Step 1: Copy the script verbatim from anker**

The script is 670 lines and repo-agnostic (it reads `tsup.config.ts` at runtime). Copy it rather than retyping:

```bash
cd /Users/jeskoiwanovski/repo/fieldkit
mkdir -p scripts
cp /Users/jeskoiwanovski/repo/anker/scripts/verify-exports.ts scripts/verify-exports.ts
```

- [ ] **Step 2: Confirm the copy landed and is unmodified**

Run: `head -5 scripts/verify-exports.ts && wc -l scripts/verify-exports.ts`
Expected: header comment beginning `/**` and `verify-exports — Asserts every source-side export...`, line count `670`.

- [ ] **Step 3: Add `tsx` devDependency and the `verify-exports` script**

In `package.json`, add `tsx` to `devDependencies` (match anker's version `^4.21.0`) and add the script. The scripts block currently ends with `"prepublishOnly": "npm run build"`; add the new script before it:

```jsonc
// in "scripts":
    "test:watch": "vitest",
    "verify-exports": "tsx scripts/verify-exports.ts",
    "prepublishOnly": "npm run build"
```

```jsonc
// in "devDependencies" (insert in alphabetical position, before "typescript"):
    "tsx": "^4.21.0",
```

- [ ] **Step 4: Install to resolve `tsx` and update the lockfile**

Run: `npm install`
Expected: completes without error; `tsx` appears under `node_modules/.bin/tsx`; `package-lock.json` updated.

Verify: `ls node_modules/.bin/tsx`
Expected: path prints (no "No such file").

- [ ] **Step 5: Run the script's built-in self-test**

Run: `npm run verify-exports -- --self-test`
Expected: a series of `ok` lines ending with `verify-exports self-test: ok`, exit code 0.

- [ ] **Step 6: Build, then run verify-exports against real dist**

Run: `npm run build && npm run verify-exports`
Expected: build succeeds, then `verify-exports: ok — 5 tsup entries match their built .d.ts` (5 = schema, editor, renderer, table, rich-text-spec), exit code 0.

> If this step reports missing symbols, that is a genuine pre-existing export-surface bug in fieldkit — STOP and report it rather than editing the ported script. The script is the oracle; the fix would be re-exporting the named symbol from the relevant barrel.

- [ ] **Step 7: Confirm typecheck and lint still pass**

Run: `npm run typecheck`
Expected: no errors.

Run: `npm run lint`
Expected: no errors. (Note: fieldkit's `lint` is `biome check src`, which does not scan `scripts/`, so the new file is not linted — this is acceptable and matches the scope decision.)

- [ ] **Step 8: Commit**

```bash
git add scripts/verify-exports.ts package.json package-lock.json
git commit -m "build: add verify-exports script (ported from anker)"
```

---

## Task 1b: Narrow the rich-text-spec barrel to mirror its public API

> Added after Task 1: porting `verify-exports` surfaced a real export-surface mismatch.
> The `rich-text-spec` entry exposes only 3 aggregate collections, but imports them from a
> barrel (`node-plugins/index.ts`) that also re-exports 20 individual plugins — which the
> script's barrel-widening heuristic then expects in the built `.d.ts` (they're absent).
> Decision (confirmed with maintainer): individual plugins stay **internal**; only the 3
> aggregates are public. Narrow the barrel so it mirrors the public API. This keeps
> `verify-exports.ts` unmodified and changes nothing for published consumers (the
> individuals were never in the published `.d.ts`). Must land before Task 2 so CI is green.

**Files:**
- Modify: `src/rich-text-spec/node-plugins/index.ts`
- Modify: `src/rich-text-spec/__tests__/node-plugins.test.ts`

- [ ] **Step 1: Narrow the barrel to the 3 public aggregates**

Replace the entire contents of `src/rich-text-spec/node-plugins/index.ts` with:

```ts
// src/rich-text-spec/node-plugins/index.ts
import type { EditorNodePlugin } from "../types";
import { builtInCoreNodePlugins } from "./core-nodes";
import { builtInMarkPlugins } from "./marks";
import { builtInMediaNodePlugins } from "./media-nodes";

// Public API: only the aggregate collections are re-exported here. Individual
// node/mark plugins remain internal to their leaf modules (core-nodes.ts,
// marks.ts, media-nodes.ts) — importing them across the module must reference
// those files directly. Keeping this barrel a 1:1 mirror of the public surface
// is what verify-exports asserts against the built .d.ts.
export { builtInMarkPlugins } from "./marks";

/** All built-in node plugins (core + media). */
export const builtInNodePlugins: EditorNodePlugin[] = [
	...builtInCoreNodePlugins,
	...builtInMediaNodePlugins,
];

/** All built-in editor plugins (marks + nodes). */
export const builtInEditorPlugins: EditorNodePlugin[] = [
	...builtInMarkPlugins,
	...builtInNodePlugins,
];
```

- [ ] **Step 2: Run the rich-text-spec tests to verify they now FAIL (barrel no longer exports individuals)**

Run: `npm test -- src/rich-text-spec/__tests__/node-plugins.test.ts`
Expected: FAIL — the test still imports `builtInCoreNodePlugins`, `builtInMediaNodePlugins`, `contentLinkPlugin`, `footnotePlugin`, `headingPlugin`, `imagePlugin`, `paragraphPlugin`, `weblinkPlugin` from `../node-plugins`, which no longer exports them (import resolves to `undefined`, assertions throw).

- [ ] **Step 3: Update the test to import individuals from the leaf files**

In `src/rich-text-spec/__tests__/node-plugins.test.ts`, replace the single import block (currently lines 3-15, importing everything from `"../node-plugins"`) with three imports — aggregates from the barrel, individuals from their leaves:

```ts
import {
	builtInEditorPlugins,
	builtInMarkPlugins,
	builtInNodePlugins,
} from "../node-plugins";
import {
	builtInCoreNodePlugins,
	headingPlugin,
	paragraphPlugin,
} from "../node-plugins/core-nodes";
import {
	builtInMediaNodePlugins,
	contentLinkPlugin,
	footnotePlugin,
	imagePlugin,
	weblinkPlugin,
} from "../node-plugins/media-nodes";
```

(Leaf membership: `headingPlugin`, `paragraphPlugin`, `builtInCoreNodePlugins` → `core-nodes.ts`; `contentLinkPlugin`, `footnotePlugin`, `imagePlugin`, `weblinkPlugin`, `builtInMediaNodePlugins` → `media-nodes.ts`. `builtInEditorPlugins`, `builtInMarkPlugins`, `builtInNodePlugins` stay on the barrel.)

- [ ] **Step 4: Run the rich-text-spec tests to verify they PASS**

Run: `npm test -- src/rich-text-spec/__tests__/node-plugins.test.ts`
Expected: PASS (all assertions green).

- [ ] **Step 5: Rebuild and run verify-exports — must now pass**

Run: `npm run build && npm run verify-exports`
Expected: `verify-exports: ok — 5 tsup entries match their built .d.ts`, exit code 0.

- [ ] **Step 6: Full check sweep**

Run: `npm run lint && npm run typecheck && npm test`
Expected: all pass (no new errors; pre-existing warnings are fine).

- [ ] **Step 7: Commit**

```bash
git add src/rich-text-spec/node-plugins/index.ts src/rich-text-spec/__tests__/node-plugins.test.ts
git commit -m "refactor(rich-text-spec): narrow node-plugins barrel to public aggregates"
```

---

## Task 2: Add the `verify-exports` step to CI

**Files:**
- Modify: `.github/workflows/ci.yml`

- [ ] **Step 1: Insert the step after `build`, before `test`**

Replace the `steps:` tail of the `check` job. Current file ends:

```yaml
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run build
      - run: npm run test
```

Change to:

```yaml
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run build
      - run: npm run verify-exports
      - run: npm run test
```

- [ ] **Step 2: Validate the YAML parses**

Run: `node -e "const f=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); require('child_process'); console.log(f.includes('npm run verify-exports') ? 'step present' : 'MISSING')"`
Expected: `step present`.

(Optional stronger check if `yq` is installed: `yq '.jobs.check.steps[].run' .github/workflows/ci.yml` lists the six `npm` commands in order.)

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: run verify-exports in CI"
```

---

## Task 3: Rework the publish workflow for anker parity + dual publish

**Files:**
- Modify: `.github/workflows/publish-fieldkit.yml`

- [ ] **Step 1: Replace the workflow file contents**

Overwrite `.github/workflows/publish-fieldkit.yml` with:

```yaml
name: Publish @knkcs/fieldkit

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read
  id-token: write
  packages: write

jobs:
  publish:
    name: Publish to npmjs.org and GitHub Packages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: https://registry.npmjs.org
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install deps
        run: npm ci

      - name: Verify version matches tag
        run: |
          PKG_VERSION=$(node -p "require('./package.json').version")
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          if [ "$PKG_VERSION" != "$TAG_VERSION" ]; then
            echo "package.json version ($PKG_VERSION) does not match tag ($TAG_VERSION)"
            exit 1
          fi

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build

      - name: Verify exports
        run: npm run verify-exports

      - name: Test
        run: npm test

      - name: Publish to npmjs.org
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: https://npm.pkg.github.com
          scope: "@knkcs"

      - name: Publish to GitHub Packages
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

- [ ] **Step 2: Validate the version-check bash logic against the new tag scheme**

Run:
```bash
GITHUB_REF_NAME="v0.0.1"; echo "stripped=${GITHUB_REF_NAME#v}"
```
Expected: `stripped=0.0.1` (confirms `${GITHUB_REF_NAME#v}` yields the bare semver, matching `package.json` `"version": "0.0.1"`).

- [ ] **Step 3: Validate the YAML structure**

Run: `node -e "const f=require('fs').readFileSync('.github/workflows/publish-fieldkit.yml','utf8'); console.log(['tags:','v*','id-token: write','packages: write','--provenance','npm.pkg.github.com'].every(s=>f.includes(s)) ? 'all markers present' : 'MISSING MARKER')"`
Expected: `all markers present`.

(Optional if `yq` is installed: `yq '.on.push.tags' .github/workflows/publish-fieldkit.yml` prints `- v*`.)

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/publish-fieldkit.yml
git commit -m "ci: dual-publish to npmjs+GitHub Packages on v* tags with full checks"
```

---

## Operator prerequisites (not code — verify before first release)

These are environment settings, not implementation steps. Confirm before pushing a `v*` tag:

- Repo secret **`NPM_TOKEN`** exists (already referenced by the prior workflow).
- GitHub Packages uses the auto-provided `GITHUB_TOKEN` — no secret needed.
- npmjs provenance requires the package to be public and the workflow to run from this repo with `id-token: write` (set) on a GitHub-hosted runner.

---

## Final verification (after all tasks)

- [ ] Run: `npm run build && npm run verify-exports -- --self-test && npm run verify-exports` — all pass.
- [ ] Run: `npm run typecheck && npm run lint` — all pass.
- [ ] Run: `git log --oneline -3` — three commits present (build, ci, ci).
- [ ] Inspect both workflow files once more; confirm `ci.yml` runs `verify-exports` and `publish-fieldkit.yml` triggers on `v*`, publishes to both registries, and gates on lint+typecheck+build+verify-exports+test.
